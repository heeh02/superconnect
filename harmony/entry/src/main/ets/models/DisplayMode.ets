// DisplayMode — how the received stream is presented, orthogonal to ConnectionStatus.
// Windowed = small in-app preview (default on connect); Fullscreen = immersive, input forwarded.
export enum DisplayMode {
  Windowed,
  Fullscreen,
}
